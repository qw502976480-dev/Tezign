
/**
 * @deprecated 
 * This component has been deprecated and removed from the active codebase.
 * It was previously used in Hero.tsx for button effects but caused stability issues.
 * This file is kept as a placeholder to prevent build errors during transition and can be safely deleted.
 */
import React from 'react';

const Magnetic: React.FC<{ children?: React.ReactNode }> = ({ children }) => {
  return <>{children}</>;
};

export default Magnetic;
